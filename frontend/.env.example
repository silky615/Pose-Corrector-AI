# Base URL of the Exercise-Correction backend (Prakriti).
# Leave empty to use same origin (e.g. when frontend is served by Django).
# Example when backend runs on port 8000: http://localhost:8000
REACT_APP_API_URL=http://localhost:8000
